import 'package:flutter/material.dart';

class SignUpInLogoWidget extends StatelessWidget {
  const SignUpInLogoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        margin: const EdgeInsets.only(top: 50),
        width: 170,
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}