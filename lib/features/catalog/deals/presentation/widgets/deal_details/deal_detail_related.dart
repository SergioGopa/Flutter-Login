import 'package:flutter/widgets.dart';

class DealDetailRelated extends StatelessWidget {
  const DealDetailRelated({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}