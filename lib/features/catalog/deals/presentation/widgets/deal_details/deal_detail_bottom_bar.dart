import 'package:flutter/widgets.dart';

class DealDetailBottomBar extends StatelessWidget {
  const DealDetailBottomBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}