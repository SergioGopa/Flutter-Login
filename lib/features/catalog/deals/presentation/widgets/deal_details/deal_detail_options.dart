import 'package:flutter/widgets.dart';

class DealDetailOptions extends StatelessWidget {
  const DealDetailOptions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}