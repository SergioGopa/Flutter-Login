import 'package:flutter/widgets.dart';

class DealDetailRestaurantCard extends StatelessWidget {
  const DealDetailRestaurantCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}