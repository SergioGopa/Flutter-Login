import 'package:flutter/widgets.dart';

class DealDetailReviews extends StatelessWidget {
  const DealDetailReviews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}